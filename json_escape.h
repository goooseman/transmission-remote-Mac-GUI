
/**
 * Function returns string with all escape sequences converted into real
 * bits of text. One have to free returned string.
 * @param json_text Escaped text.
 * @retrun Converted text.
 */
const char *convert_from_escape(const char *json_text);
